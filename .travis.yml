language: cpp
compiler:
  - clang
  - gcc

os:
  - linux
  - osx

script:
  - cmake --version
  - export CC=gcc-7
  - export CXX=g++-7
  - mkdir src/build
  - cd src/build
  - cmake ..
  - cmake  --build .

branches:
  only:
  - master

notifications:
  email: false
