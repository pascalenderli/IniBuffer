language: cpp
compiler:
  - clang
  - gcc

os:
  - linux
  - osx
  - windows

script:
  - cmake --version
  - mkdir src/build
  - cd src/build
  - cmake ..
  - cmake  --build .
  - ls ..
  - ls

branches:
  only:
  - master

notifications:
  email: false
